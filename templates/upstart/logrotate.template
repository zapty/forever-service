/var/log/{{service}}.log {
    {{logrotateFrequency|default('daily')}}
    rotate {{logrotateMax|default('10')}}
    missingok
    notifempty
    copytruncate
    {{dateExt|default('')}}
    {{logrotateCompress|default('')}}
}